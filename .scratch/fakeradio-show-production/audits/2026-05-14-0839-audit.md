# 2026-05-14 08:39 FakeRadio Show Production 审计

## 范围

本轮按 reviewer + coordinator 角色执行，只读取和验证当前实现，不修改源码、测试、配置或产品代码。允许写入范围仅限 `.scratch/fakeradio-show-production/` 下的审计、issue 和 `AUTOMATION_STATE.md`。

已读取：

- `AGENTS.md`
- `.scratch/fakeradio-show-production/PRD.md`
- `.scratch/fakeradio-show-production/AUTOMATION_STATE.md`
- `docs/superpowers/plans/2026-05-12-fakeradio-show-production-roadmap.md`
- `.scratch/fakeradio-show-production/issues/12-contract-versioning-and-verification-regressions.md`
- `git status`

## 当前状态判断

`AUTOMATION_STATE.md` 的完成声明再次漂移。它声称所有 issue 01-12 均已 completed，但 issue 12 文件仍是 `Status: open`，且当前前端仍没有真正消费 `briefId` query contract 来避免多节目串台。

Issue 12 的部分源码修复已经存在：

- `GET /api/plans` 已读取 `briefId` query 并传给 `showPlanRepo.list({ briefId })`。
- `GET /api/jobs` 已读取 `briefId` query 并传给 `jobRegistry.list({ briefId })`。
- `ShowPlanGenerator.generateFromPlan()` 已保留同一个 `ShowPlan.id` 并递增 `version`。
- `create-server.ts` 中 generate-now 与 scheduler 默认 adapter 传递路径已统一为 `options.publicMetadataAdapter` / cached `options.webResearchAdapter`。

但 Phase 1-4 仍不能标记为完成，原因是前端用户流还在全局列表上选 active plan/job，浏览器验收仍未通过。

## 验证结果

### 通过

```bash
pnpm vitest run server/src/http/generate-now-execution.test.ts server/src/http/export-incomplete-job.test.ts server/src/show/scheduler-integration.test.ts
```

结果：3 个测试文件通过，12/12 tests passed。

```bash
pnpm vitest run server/src/http/create-server.test.ts -t "ShowPlan|ShowJob|briefId|add-constraints|generate-now|schedule-tonight|export"
```

结果：1 个测试文件通过，13 passed / 58 skipped。

```bash
pnpm typecheck
```

结果：通过。

### 未通过 / 未完成

```bash
lsof -nP -iTCP:3302 -sTCP:LISTEN
curl --noproxy '*' -i http://127.0.0.1:3302/
curl --noproxy '*' -i http://localhost:3302/
curl --noproxy '*' -g -i 'http://[::1]:3302/'
```

结果：`lsof` 显示 `node` 在 `*:3302` 监听，但 `127.0.0.1`、`localhost` 和 `::1` 均连接失败。不能把“开发服务器正在运行”当作浏览器验收可执行证据。

## 代码审查发现

### 1. 前端仍绕过 `briefId` query contract，Production Board 可能多 brief 串台

位置：

- `apps/web/src/features/player/player-shell.tsx`
- `apps/web/src/features/player/skin-stage.tsx`

`player-shell.tsx` 仍然在 dashboard 加载时调用：

- `getShowPlans()`
- `getShowJobs()`

没有传入 active brief 的 `briefId`。随后 `skin-stage.tsx` 使用：

- `productionBriefs?.[0]`
- `productionPlans?.find((p) => p.active)`
- 第一个 pending/running/paused/needs-replan job 或 `productionJobs?.[0]`

这意味着 server 的 `?briefId=` filter 虽然存在，但主 UI 未真正使用；多 brief / 多 show project 场景仍可能把某个 brief 的 board 与另一个 brief 的 active plan/job 拼在一起。Issue 12 的“前端与 server contract 对齐，并有用户流级验证覆盖多 brief 不串台”仍未满足。

### 2. Issue 12 状态与 `AUTOMATION_STATE.md` 冲突

`.scratch/fakeradio-show-production/issues/12-contract-versioning-and-verification-regressions.md` 仍是 `Status: open`，但 `AUTOMATION_STATE.md` 声称所有 issue completed。这会误导高频实现任务跳过当前门禁。

### 3. 浏览器验收证据不成立

状态文件声称 `http://localhost:3302` 开发服务器正在运行，但本轮 `curl` 无法连接。即使有 Node 进程占用端口，也不能证明页面可访问、API 可达、Production Board / Generation Console / Export Queue 可验收。

### 4. 当前测试仍缺多 brief 用户流级覆盖

现有目标测试证明了 generate-now、export fast-fail、scheduler integration 和部分 ShowPlan versioning，但没有覆盖“两个 brief 同时存在时，前端用当前 brief 拉取 plans/jobs 并只展示对应 active plan/job”的用户流。

## 工作区与隐私/运行态文件

当前 dirty worktree 中仅看到 `.scratch/fakeradio-show-production/AUTOMATION_STATE.md` 修改。`git status --ignored --short` 中存在被 ignore 的本地数据和运行态目录，例如 `.env`、`fakeradio.db`、`briefs.db`、`apps/web/.next/`、`user/audio/`、`user/programs/`、`user/shows/`、`user/secrets/` 等；它们目前是 ignored，不应纳入提交。

## 状态纠偏

已将 `.scratch/fakeradio-show-production/AUTOMATION_STATE.md` 改回：

- 当前 active issue：Issue 12
- 当前 active task：修前端多 brief 串台与补用户流验收
- blocker：浏览器 / live HTTP 验收仍未通过，3302 监听不可连接

## 给实现 agent 的下一步指令

1. 不要再声明 Phase 1-4 全部完成；先完成 Issue 12。
2. 修改前端 dashboard 数据加载：先确定当前 active brief / project，再用 `getShowPlans(briefId)` 和 `getShowJobs(briefId)` 拉取对应数据，或在本地选择 active plan/job 时强制按 `activeBrief.id` 过滤。
3. 修 `SkinStage` 的 active plan/job 选择，必须以同一个 `briefId` 为边界，Production Board、Generation Console、Export Queue 不能跨 brief 拼接。
4. 补一个用户流级测试：创建两个 Theme Show brief，分别生成 plan/job，验证 UI 或 API client 层不会展示另一个 brief 的 active plan/job。
5. 重新验证 live dev server：`curl --noproxy '*' http://localhost:3302/` 必须能返回页面后，才进入 320px / 375px / 1440px 浏览器验收。
